cryptography>=50.0.1
fastapi>=0.141.1
uvicorn>=0.52.4
pydantic>=2.13.5

# Only needed to run the intent check against the real model
# (ANTHROPIC_API_KEY set). Everything else runs without it.
anthropic>=1.2.0
